# Automatically created by: shub deploy

from setuptools import setup, find_packages

setup(
    name='autos_prefeitura_sp',
    version='1.0',
    packages=find_packages(),
    entry_points={'scrapy': ['settings = brobot_bots.settings']}
)
